# frozen_string_literal: true

source "https://rubygems.org"

gemspec

gem 'jekyll-sitemap'
gem 'jekyll-seo-tag'

# Bundled gems no longer in Ruby's default load path (needed on Ruby >= 3.4)
gem 'bigdecimal'
